/// <reference types="react" />
export declare type CardDatesProps = {
    variant?: 'dark' | 'light'
    children: string
    moreOptions: string
}
export declare const CardDates: ({
    variant,
    children,
    moreOptions,
}: CardDatesProps) => JSX.Element
export default CardDates
